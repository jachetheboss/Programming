#include <iostream>
#include <fstream>
#include <vector>
#include <string>
#include <algorithm>

using namespace std;

// idea for Bellman-Ford:
// traversal from vertex start to vertex destination, find optimal weighting
// for each "distance" of travel (not counting edge weights, just number of edges).
// Then, recalculate optimal path costs?

int main(){
	
	// draw a few graphs, think of a few examples of graphs, cycles, etc.
	// only start with positive weight edges, easier!
	
	
	
	return 0;
}